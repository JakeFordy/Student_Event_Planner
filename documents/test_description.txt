1. test_unbounded_bug.txt

Expected value: 10

Ensures correct 0/1 knapsack behaviour
    Each item is used once

cost = 1, enjoyment = 10, budget = 6
incorrect result = 60 (6*1 = 6, so 6*10 = 60)
    means object was reused
correct result = 1
    object was not reused

Catches - forward DP iteration, unbounded knapsack logic, reusing items

2. test_exact_fit.txt

Expected value: 55

Confirms optimal combination selection and exact budget fit

Budget = 10 
A(cost = 10, enjoyment = 50)
B(cost = 6, enjoyment = 35)
c(cost = 4, enjoyment = 20)

So optimal solution = B + C (55)

Catches - Greedy mistakes, missed combinations, off-by-one capacity errors, incorrect dp transitions

3. test_tie.txt

Expected value: 20

Handles multiple optimal solutions correctly

A + B = 20
C = 20

Only the max value matters

Catches - Incorrect max compatrisons, dp overwrite errors, tie-handling issues

4. test_none_fesible.txt

Handles cases where no activity fits the budget

Catches - bad dp initialization, infinity returns, crashes on edge cases

5. test_tradeoff.txt

Ensures time constraint is ignored in cost-only mode

budget = 10
Best = (6, 70) + (4, 40) = 110

Catches - accidentally enforcing time, incorrect feasibility checks, combination errors

6. test_both_binding.txt

Best budget-onlt result

Confirms - time is ignored
         - budget-optimal subset is chosen

Catches - enforcing time accidentally, cost/time parsing mistakes

